# Ultimate Sensor Monitor – Front-end environment variables
# Copy this file to `.env` and adjust as needed.

# Base URL for REST API (FastAPI backend)
VITE_API_BASE_URL=http://localhost:8100

# WebSocket URL for real-time updates
VITE_WEBSOCKET_URL=ws://localhost:8100/ws

# Optional: Override proxy target in Vite dev server (defaults to VITE_API_BASE_URL)
# API_PROXY_TARGET=http://localhost:8100
